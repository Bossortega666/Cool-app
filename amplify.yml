version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # <-- Usa 'dist' para Vite, o cámbialo si es 'build'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
